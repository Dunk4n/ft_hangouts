<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Ft_hangouts" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@null</item>
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/buttonBackground</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <style name="Theme.Alliance" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@null</item>
        <item name="colorPrimary">@color/colorAllianceLight</item>
        <item name="colorSecondary">@color/colorAlliance</item>
        <item name="colorPrimaryDark">@color/colorAllianceDark</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <style name="Theme.Federation" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@null</item>
        <item name="colorPrimary">@color/colorFederationLight</item>
        <item name="colorSecondary">@color/colorFederation</item>
        <item name="colorPrimaryDark">@color/colorFederationDark</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <style name="Theme.Order" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@null</item>
        <item name="colorPrimary">@color/colorOrderLight</item>
        <item name="colorSecondary">@color/colorOrder</item>
        <item name="colorPrimaryDark">@color/colorOrderDark</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <style name="Theme.42" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@null</item>
        <item name="colorPrimary">@color/color42Light</item>
        <item name="colorSecondary">@color/color42</item>
        <item name="colorPrimaryDark">@color/color42Dark</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <style name="Theme.Ft_hangouts.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Ft_hangouts.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Ft_hangouts.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>